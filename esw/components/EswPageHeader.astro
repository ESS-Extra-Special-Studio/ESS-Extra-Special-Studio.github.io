---
type Props = {
  title: string;
  lede: string;
};

const { title, lede } = Astro.props as Props;
---

<header class="page-header">
  <h1 class="page-title">{title}</h1>
  <p class="lede">{lede}</p>
</header>
