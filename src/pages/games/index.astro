---
import BaseLayout from "../../layouts/BaseLayout.astro";
import EswPageHeader from "../../../esw/components/EswPageHeader.astro";
import EswEmptyState from "../../../esw/components/EswEmptyState.astro";
import { games } from "../../data/games";
---

<BaseLayout
  title="Games"
  description="Extra Special Studio games. Titles appear here only when they are public."
  path="/games"
>
  <EswPageHeader
    title="Games"
    lede="Extra Special Studio also makes games. Nothing is listed here until it is meant to be public. Nothing here is marketed as a shipped store title until it actually is."
  />
  {
    games.length > 0 ? (
      <div class="grid">
        {games.map((game) => (
          <a class="card project-card" href={`/games/${game.id}`}>
            <div class="icon-ph" aria-hidden="true">
              {game.status}
            </div>
            <div>
              <h2>{game.name}</h2>
              <p>{game.short}</p>
              <span class="status">{game.status}</span>
            </div>
          </a>
        ))}
      </div>
    ) : (
      <EswEmptyState message="No public games to show yet." />
    )
  }
</BaseLayout>
